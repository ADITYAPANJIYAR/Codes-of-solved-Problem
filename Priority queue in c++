#include <bits/stdc++.h>
using namespace std;
int main()
{
    priority_queue<int>q;
    cout<<"Enter number of elements"<<endl;
    int n;
    cin>>n;
    
    while(n)
    {
        n--;
        int x;
        cin>>x;
        q.push(x);
    }
    
    while(!q.empty())
    {
        cout<<q.top()<<endl;
        q.pop();
    }
}
